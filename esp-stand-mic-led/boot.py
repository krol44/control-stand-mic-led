# This is script that run when device boot up or wake from sleep.

# blueled = machine.Pin(2, machine.Pin.OUT) 
# def ledNoty():
#     print ("..")
#     blueled.value(1)
#     sleep(0.5)
#     blueled.value(0)